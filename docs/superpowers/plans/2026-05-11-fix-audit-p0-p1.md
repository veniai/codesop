# Fix: Audit P0+P1 Findings Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix 3 P0 bugs (sed injection, untracked stash, dead reference) and 9 P1 documentation drift items identified in full project audit.

**Architecture:** P0 fixes are code changes in lib/ and patches/. P1 fixes are documentation synchronization across SKILL.md, PRD.md, CLAUDE.md, README.en.md. No refactoring, no new functions.

**Tech Stack:** Bash, Markdown

---

### Task 1: Fix sed injection in template generation (P0 #1)

**Files:**
- Modify: `lib/init-interview.sh:429,445`

- [ ] **Step 1: Escape project_name for sed in generate_prd_template**

Replace line 429 in `lib/init-interview.sh`:

```bash
    sed "s/{PROJECT_NAME}/$project_name/g; s/{DATE}/$date_today/g" "$template_file"
```

with:

```bash
    local escaped_name
    escaped_name=$(printf '%s' "$project_name" | sed 's/[&/\]/\\&/g')
    local escaped_date
    escaped_date=$(printf '%s' "$date_today" | sed 's/[&/\]/\\&/g')
    sed "s/{PROJECT_NAME}/$escaped_name/g; s/{DATE}/$escaped_date/g" "$template_file"
```

- [ ] **Step 2: Escape project_name for sed in generate_readme_template**

Replace line 445 in `lib/init-interview.sh`:

```bash
    sed "s/{PROJECT_NAME}/$project_name/g" "$template_file"
```

with:

```bash
    local escaped_name
    escaped_name=$(printf '%s' "$project_name" | sed 's/[&/\]/\\&/g')
    sed "s/{PROJECT_NAME}/$escaped_name/g" "$template_file"
```

- [ ] **Step 3: Run tests**

Run: `bash tests/codesop-init-interview.sh 2>&1 | tail -5`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add lib/init-interview.sh
git commit -m "fix: escape sed special chars in project name template substitution"
```

### Task 2: Fix untracked file stash handling (P0 #8)

**Files:**
- Modify: `lib/commands.sh:120`

- [ ] **Step 1: Add -u flag to git stash for untracked files**

Replace line 120 in `lib/commands.sh`:

```bash
    git stash push -m "codesop-update-$(date +%s)" 2>/dev/null || {
```

with:

```bash
    git stash push -u -m "codesop-update-$(date +%s)" 2>/dev/null || {
```

- [ ] **Step 2: Run tests**

Run: `bash tests/codesop-update.sh 2>&1 | tail -5`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add lib/commands.sh
git commit -m "fix: include untracked files in stash during codesop update"
```

### Task 3: Remove executing-plans dead reference from patches (P0 #11)

**Files:**
- Modify: `patches/superpowers/writing-plans-SKILL.md:63`

- [ ] **Step 1: Remove executing-plans fallback from plan header**

Replace line 63 in `patches/superpowers/writing-plans-SKILL.md`:

```markdown
> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
```

with:

```markdown
> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
```

- [ ] **Step 2: Resync and verify**

Run: `bash setup --host claude 2>&1 | tail -5`
Expected: All steps succeed

Run: `grep "executing-plans" ~/.claude/plugins/cache/claude-plugins-official/superpowers/*/skills/writing-plans/SKILL.md`
Expected: No output (dead reference removed)

- [ ] **Step 3: Commit**

```bash
git add patches/superpowers/writing-plans-SKILL.md
git commit -m "fix: remove deprecated executing-plans reference from writing-plans patch"
```

### Task 4: Sync SKILL.md documentation drift (P1 #3, #10)

**Files:**
- Modify: `SKILL.md:100,348-354`

- [ ] **Step 1: Fix step 9 deprecated executing-plans reference**

Replace line 100 in `SKILL.md`:

```
When multiple skills match, prefer ★-marked skills (e.g. subagent-driven-development over executing-plans). After assembling the chain, apply the **链路完整性** principle: check for logical gaps between adjacent skills (e.g. code-review without receiving-code-review, feedback without fix-and-verify), and fill them before outputting.
```

with:

```
When multiple skills match, prefer ★-marked skills (e.g. subagent-driven-development over other options). After assembling the chain, apply the **链路完整性** principle: check for logical gaps between adjacent skills (e.g. code-review without receiving-code-review, feedback without fix-and-verify), and fill them before outputting.
```

- [ ] **Step 2: Add uninstall to §8 Sub-commands table**

Replace lines 348-354 in `SKILL.md` (the §8 table):

```markdown
## 8. Sub-commands

| Command | Run | What it does |
|---------|-----|-------------|
| `/codesop init [path]` | `codesop init <dir>` | Generate AGENTS.md (`@CLAUDE.md`), PRD.md (活文档), README.md (if missing). Defaults to 中文. |
| `/codesop update` | `codesop update` | Check plugin versions → show status → resync host integration. |
```

with:

```markdown
## 8. Sub-commands

| Command | Run | What it does |
|---------|-----|-------------|
| `/codesop init [path]` | `codesop init <dir>` | Generate AGENTS.md (`@CLAUDE.md`), PRD.md (活文档), README.md (if missing). Defaults to 中文. |
| `/codesop update` | `codesop update` | Check plugin versions → show status → resync host integration. |
| `/codesop uninstall` | `codesop uninstall` | Remove codesop integration (keeps installed plugins). |
```

- [ ] **Step 3: Run tests**

Run: `bash tests/detect-environment.sh 2>&1 | tail -3`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add SKILL.md
git commit -m "fix: add uninstall to §8 subcommands, remove executing-plans reference"
```

### Task 5: Sync PRD.md documentation drift (P1 #2, #4, #9)

**Files:**
- Modify: `PRD.md:22,97,188-198`

- [ ] **Step 1: Update milestone version**

Replace line 22 in `PRD.md`:

```
- **当前里程碑**: v3.10.0 uninstall 子命令
```

with:

```
- **当前里程碑**: v3.10.1 稳定维护
```

- [ ] **Step 2: Fix command count**

Replace line 97 in `PRD.md`:

```
- 两个机械命令：`codesop init`、`codesop update`
```

with:

```
- 三个机械命令：`codesop init`、`codesop update`、`codesop uninstall`
```

- [ ] **Step 3: Update document judgment format**

Replace lines 193-198 in `PRD.md`:

```
- 输出格式:
  ```
  - CLAUDE.md: 已更新 / 未更新，原因：...
  - PRD.md: 已更新 / 未更新，原因：...
  - README.md: 已更新 / 未更新，原因：...
  ```
```

with:

```
- 输出格式见 SKILL.md §5 Completion Gate（☐/☑ 可视化格式，含 CONTEXT.md 和 ADR 条件行）
```

- [ ] **Step 4: Commit**

```bash
git add PRD.md
git commit -m "fix: sync PRD — command count, version, document judgment format"
```

### Task 6: Sync CLAUDE.md documentation drift (P1 #5, #12)

**Files:**
- Modify: `CLAUDE.md:64,88`

- [ ] **Step 1: Fix Phase 4 description**

Replace line 88 in `CLAUDE.md`:

```
| 4 | CLI | Auto-install managed dependencies (`install_managed_deps` from manifest) |
```

with:

```
| 4 | CLI | Check skill dependencies (`check_skill_dependencies`) |
```

- [ ] **Step 2: Fix patch filename description**

Replace line 64 in `CLAUDE.md`:

```
│   └── superpowers/        # Modified superpowers skill files (writing-plans, finishing-branch, brainstorming)
```

with:

```
│   └── superpowers/        # Modified superpowers skill files (writing-plans, finishing-a-development-branch, brainstorming)
```

- [ ] **Step 3: Run tests**

Run: `bash tests/detect-environment.sh 2>&1 | tail -3`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add CLAUDE.md
git commit -m "fix: correct Phase 4 description and patch filename in CLAUDE.md"
```

### Task 7: Sync README.en.md (P1 Codex finding)

**Files:**
- Modify: `README.en.md`

- [ ] **Step 1: Add uninstall to English README**

Find the commands section in `README.en.md`. Add uninstall command alongside existing init and update entries, matching the Chinese README.md format at line 43 and line 120.

Read the file first to find exact insertion points, then add:
- In the commands overview section, add `/codesop uninstall — Remove codesop integration (keeps installed plugins)`
- In the detailed commands section, add the uninstall command block matching the pattern of init/update

- [ ] **Step 2: Run tests**

Run: `bash tests/run_all.sh 2>&1 | tail -3`
Expected: 10 passed, 0 failed

- [ ] **Step 3: Commit**

```bash
git add README.en.md
git commit -m "fix: add uninstall command to English README"
```

### Task 8: Full verification

**Files:** None

- [ ] **Step 1: Run full test suite**

Run: `bash tests/run_all.sh 2>&1`
Expected: 10 passed, 0 failed

- [ ] **Step 2: Resync host**

Run: `bash setup --host claude 2>&1`
Expected: All steps succeed

- [ ] **Step 3: Verify P0 fixes**

```bash
# P0 #1: sed escape
grep -c 'escaped_name' lib/init-interview.sh
# Expected: 2

# P0 #8: stash -u
grep 'stash push -u' lib/commands.sh
# Expected: match

# P0 #11: no executing-plans in installed patch
grep "executing-plans" ~/.claude/plugins/cache/claude-plugins-official/superpowers/*/skills/writing-plans/SKILL.md
# Expected: no output
```
